/* Incluir o agregador `Sistema` diretamente e testar uso em tempo de execução. */
#Inclua "../../../Fonte/Sistema/Sistema.ctec"

Externo Vazio ctec_inicialize_sistema(Vazio);

Inteiro Início() {
	/* Inicializa o agregador Sistema em tempo de execução, então usa-o */
	Sistema.terminal->exiba_texto("teste_via_sistema_exiba_texto\n");
	Retorne 0;
}
